node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*.local
.DS_Store

# State lives outside the tree (DATA_DIR); nothing here is deployable data.
data/

# Windows alternate-data-stream artifacts. `.env*.local` does not match
# `.env.local:Zone.Identifier` because of the suffix, and it slipped into a
# commit once.
*Zone.Identifier
